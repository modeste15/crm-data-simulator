# CRM POC — FastAPI + PostgreSQL (Docker) + Seeders + Jobs

POC d’une simulation de base de données CRM avec :
- **FastAPI** (API REST)
- **PostgreSQL** (DB)
- **SQLAlchemy** (ORM)
- **Seeders** 
- **Jobs** (APScheduler : création automatique d’actions + parfois devis/vente)

